import numpy as np

# Define the number of samples and targets
number_of_samples = 1000  # Adjust as needed
number_of_targets = 10    # Adjust based on the number of target variables

# Generate random data for training targets
train_targets = np.random.rand(number_of_samples, number_of_targets)

# Save the array to a .npy file
np.save('data/train_targets.npy', train_targets)

print("Generated and saved train_targets.npy")
